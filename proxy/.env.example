# Copy to .env (never commit the real file). Railway: set these as service Variables.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
MODEL=claude-haiku-4-5
PORT=8787
ALLOWED_ORIGIN=https://web.whatsapp.com
# Optional shared secret. If set, the extension must send the same value as
# x-pfc-key (set it in the extension's Options page). Stops random callers.
SHARED_SECRET=
